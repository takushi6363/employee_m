<h3>社員情報一覧</h3>
 <small>社員の一覧になります。編集と削除が可能です。</small>
</h3>
<div class="breadcrumb-line">
    <ul class="breadcrumb">
      <li class="li-1">
       <%= link_to "管理画面トップ", master_index_path,class:"breadcrumb-sen"%>
      </li>
      <li class="slash">
      /
      <li>
      <li class="li-a"> 社員情報一覧 </li>
</div>

 <div class="paid-kanri">
   <div>
   <%= link_to "社員の新規登録", new_user_path,class:"paid-kanri-btn"%>
   </div>
   <div>
   <%= link_to "マスター管理画面に戻る", master_index_path,class:"master-kanri-btn"%>
   </div>
 </div>

<div class="table-panel-body adding-bottom">
 <table>
   <thead class="thead-employee">
      <tr class="tr-enployee">
        <th>名前</th>
        <th>住所</th>
        <th>生年月日</th>
        <th>電話番号</th>
        <th>電話番号 2</th>
        <th>入社日</th>
        <th>Action</th>
      </tr>
   </thead>
    <% @user.each do |user| %>
    <tbody>
      <tr class="tr-enployee">
        <th><%= user.name%></th>
        <th><%= user.address%></th>
        <th><%= user.birthday%></th>
        <th><%= user.phone_number%></th>
        <th><%= user.phone_number_2%></th>
        <th><%= user.joining_day%></th>
        <th><%= link_to '削除', user_path(user.id), method: :delete,data: { confirm: '削除しますか？' } %>
            <%= link_to '編集', edit_user_path(user.id) %>
        </th>
      </tr>
    </tbody>
  <% end %>
 </table>
</div>

